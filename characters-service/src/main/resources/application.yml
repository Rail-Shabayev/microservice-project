spring:
  application.name: characters-service
  datasource:
    url: jdbc:postgresql://localhost:5432/characters
    username: postgres
    password: password
  jpa:
    show-sql: true
    hibernate.ddl-auto: create-drop